// 01 - Utilize o método forEach para imprimir cada elemento de um array juntamente com seu índice.

const elementos = ['A', 'B', 'C', 'D', 'E'];

elementos.forEach((letra, index) =>
    console.log(`${index} - ${letra}`

    ));


